export enum ContextDomain {
  ONDC_RET11 = 'ONDC:RET11',
}

export enum ContextAction {
  ON_SEARCH = 'on_search',
}

export enum ContextCountry {
  IND = 'IND',
}

export enum CoreVersion {
  V1_2_0 = '1.2.0',
  V1_2_5 = '1.2.5',
}

export enum FulfillmentType {
  DELIVERY = 'Delivery',
  SELF_PICKUP = 'Self-Pickup',
}

export enum TimeLabel {
  ENABLE = 'enable',
  DISABLE = 'disable',
}

export enum QuantityAvailableCount {
  NINETY_NINE = '99',
  ZERO = '0',
}

export enum PriceCurrency {
  INR = 'INR',
}

export enum TimingType {
  SELF_PICKUP = 'Self-Pickup',
  ORDER = 'Order',
  DELIVERY = 'Delivery',
  ALL = 'All',
}

export enum CatalogLinkType {
  INLINE = 'inline',
  LINK = 'link',
}
